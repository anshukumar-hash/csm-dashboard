name: Daily CS Report to Slack

on:
  schedule:
    # Target ~9:30 AM IST, ~30 min after the email. Fire EARLY on an odd minute
    # to absorb GitHub's best-effort cron delay (queue is busiest at :00/:30).
    # 03:37 UTC = 9:07 AM IST (IST = UTC+5:30).
    - cron: '37 3 * * *'
  workflow_dispatch: {}

concurrency:
  group: cs-report-slack
  cancel-in-progress: false

jobs:
  slack:
    runs-on: ubuntu-latest
    env:
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
      SLACK_CHANNEL:   ${{ secrets.SLACK_CHANNEL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies + Chromium
        working-directory: email
        run: |
          npm install
          npx playwright install --with-deps chromium

      - name: Screenshot the Email View and post to Slack
        working-directory: email
        run: node shoot-and-slack.mjs
